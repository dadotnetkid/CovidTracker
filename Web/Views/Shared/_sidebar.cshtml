<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Sidebar - Brand -->
    @Html.Partial("_brand")

    <!-- Divider -->
    <hr class="sidebar-divider my-0">

    <!-- Nav Item - Dashboard -->
    <li class="nav-item">
        <a class="nav-link" href="/">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>
    <!-- Divider -->
    <hr class="sidebar-divider my-0"/>
    <li class="nav-item">
        <a class="nav-link" href="#" data-toggle="collapse" data-target="#patientsUtilities" aria-expanded="true" aria-controls="patientsUtilities">
            <i class="fas fa-fw fa-hospital-user"></i>
            <span>Patients</span>
        </a>
        <div id="patientsUtilities" class="collapse " aria-labelledby="headingUtilities" data-parent="#accordionSidebar" style="">
            <div class="bg-white py-2 collapse-inner rounded">
                <h6 class="collapse-header">Patients:</h6>
                <a class="collapse-item" href="@Url.Action("Batch", "Patients")">Batch Number</a>
     
            </div>
        </div>
    </li>
    <!-- Divider -->
    <hr class="sidebar-divider my-0"/>
    @* Divider *@
    @if (User.UserIsInAction("Users"))
    {
        <li class="nav-item">
            <a class="nav-link" href="#" data-toggle="collapse" data-target="#collapseUtilities" aria-expanded="true" aria-controls="collapseUtilities">
                <i class="fas fa-fw fa-wrench"></i>
                <span>Utilities</span>
            </a>
            <div id="collapseUtilities" class="collapse " aria-labelledby="headingUtilities" data-parent="#accordionSidebar" style="">
                <div class="bg-white py-2 collapse-inner rounded">
                    <h6 class="collapse-header">Utilities:</h6>
                    <a class="collapse-item" href="@Url.Action("Users", "Users")">Users</a>
                    <a class="collapse-item" href="@Url.Action("UserRoles", "Users")">User Roles</a>
                </div>
            </div>
        </li>
        <!-- Divider -->
        <hr class="sidebar-divider"/>
    }



</ul>
